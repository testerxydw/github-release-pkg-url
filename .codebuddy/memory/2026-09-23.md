# 2026-09-23

## release: Trae SOLO CN 0.1.69-7 (traework)
- 上传 `trae-solo-cn_0.1.69-7_amd64.deb`（316M）到固定 tag **`traework`** 的 Release，
  并更新 Release 标题/说明（`gh release upload` + `gh release edit --title/--notes-file`；上传耗时约 2m52s）。
- 索引增量更新：`github-release-pkg.txt` 末尾追加
  `# 2026-09-23 15:12 Trae SOLO 国内版 0.1.69-7 (amd64)` + 下载链接（遵循"只追加不覆盖、最新在末尾"约定）。
- 文档同步：
  - `README.md` 版本号 `0.1.67-10` → `0.1.69-7`
  - `github-release-pkg.md` 的 Trae 段更新为 `0.1.69-7`（版本/安装包/下载地址），
    修复列表换成「畸形订阅包不再中断流 / work shell 策略改 shell_exec / 完成通知 60s 去重 / 打包默认限 8 核」，
    原 0.1.67-10 的三条降级为「历史修复（0.1.67-10）」
- 提交：`cbdaa35 release: Trae SOLO CN 0.1.69-7 (traework)`（3 文件），
  推送 `github` 与 `origin`(gitee) 均成功（本次直连 github 顺利，未遇 443 超时）。
- 链接校验：新包与旧包 `releases/download/traework/...` HEAD 均返回 **HTTP 200**。
- 待办提示：`known-good/` 等本地文件与本仓库无关；后续如再发 Trae 新版本，仍沿用 `traework` 单 tag 累积资产的方式。
